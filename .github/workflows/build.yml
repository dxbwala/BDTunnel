APP_KEYSTORE_BASE64 }}      - name: Build APK       run: |         cd ${{ github.workspace }}/V2rayNG         echo "sdk.dir=${ANDROID_HOME}" > local.properties         chmod 755 gradlew         ./gradlew licenseFdroidReleaseReport         ./gradlew assembleRelease -Pandroid.injected.signing.store.file=${{ steps.android_keystore.outputs.filePath }} -Pandroid.injected.signing.store.password=${{ secrets.APP_KEYSTORE_PASSWORD }} -Pandroid.injected.signing.key.alias=${{ secrets.APP_KEYSTORE_ALIAS }} -Pandroid.injected.signing.key.password=${{ secrets.APP_KEY_PASSWORD }}          - name: Upload arm64-v8a APK       uses: actions/upload-artifact@v4.6.2       if: ${{  success() }}       with:         name: arm64-v8a         path: ${{ github.workspace }}/V2rayNG/app/build/outputs/apk/*/release/*arm64-v8a*.apk      - name: Upload armeabi-v7a APK       uses: actions/upload-artifact@v4.6.2       if: ${{  success() }}       with:         name: armeabi-v7a         path: ${{ github.workspace }}/V2rayNG/app/build/outputs/apk/*/release/*armeabi-v7a*.apk      - name: Upload x86 APK       uses: actions/upload-artifact@v4.6.2       if: ${{  success() }}       with:         name: x86-apk         path: ${{ github.workspace }}/V2rayNG/app/build/outputs/apk/*/release/*x86*.apk      - name: Upload x86_64 APK       uses: actions/upload-artifact@v4.6.2       if: ${{  success() }}       with:         name: x86_64-apk         path: ${{ github.workspace }}/V2rayNG/app/build/outputs/apk/*/release/*x86_64*.apk      - name: Upload universal APK       uses: actions/upload-artifact@v4.6.2       if: ${{  success() }}       with:         name: universal-apk         path: ${{ github.workspace }}/V2rayNG/app/build/outputs/apk/*/release/*universal*.apk    release:     needs: build     runs-on: ubuntu-latest     if: github.event.inputs.release_tag != ''          steps:     - name: Download all artifacts       uses: actions/download-artifact@v4       with:         path: apks              - name: Create Release       uses: softprops/action-gh-release@v2       with:         tag_name: ${{ github.event.inputs.release_tag }}         name: Release ${{ github.event.inputs.release_tag }}         draft: false         prerelease: true         files: apks/**/*.apk
